(ns me.untethr.nostr.common.version)

;; note: this value is swapped during release so don't change it w/o changing
;; release script - should be "0.0.0-release-test" whenever checked-in
(def version "SNAPSHOT")
